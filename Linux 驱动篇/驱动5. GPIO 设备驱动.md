# 一、CONCTRL 子系统

# 二、GPIO 子系统

# 三、编写驱动

## 3.1  修改设备树
打开设备树文件 `imx6ull-alientek-emmc.dts` 添加项目的根节点 gpioled（也是系统的一级子节点），设置根节点 gpioled 属性：
```
	gpioled{
		pinctrl-names = "default";
		pinctrl-0 = <&pinctrl_gpioled>; /* 子节点 */
		compatible = "alientek,gpioled";
		led-gpios = <&gpio1 3 GPIO_ACTIVE_LOW>;
		status = "okay";
	};
```
其中，在根节点下在定义子节点子`pinctrl_gpioled`：
 ```
	pinctrl_gpioled: ledrp {
			fsl,pins = <
				MX6UL_PAD_GPIO1_IO03__GPIO1_IO03    0X10B0
			>;
		};
```


## 3.2  驱动编写
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTEyOTUyMDk3OTgsLTczMDgzMDg4MiwxNj
k2MTgwNDg4XX0=
-->